# Aether Backend - Production Dependencies
# Install with: pip install -r requirements.txt

# Core scientific stack - pinned first
# Using numpy<2.0 for onnxruntime compatibility (required by qwen-tts)
numpy==1.26.4
scipy==1.14.1
llvmlite==0.43.0  # Required for numba Python 3.11 compatibility
numba==0.60.0  # Python 3.11 compatible - install before librosa

# Pin transitive deps to prevent pip backtracking
contourpy==1.3.0  # Required by matplotlib, prevent backtracking
fsspec==2025.3.0  # Required by lightning, prevent backtracking
torchmetrics==1.6.3  # Required by pyannote.audio, prevent backtracking
lightning==2.5.0  # Required by pyannote.audio, prevent backtracking

# Core Framework
fastapi==0.109.0
uvicorn==0.34.2  # Must be >=0.31.1 for mcp 1.20.0
python-multipart==0.0.9  # Must be >=0.0.9 for mcp 1.20.0

# HTTP Client - pinned to satisfy mcp>=0.27.1 and supabase<0.28
httpx==0.27.2

# Settings Management - pinned for mcp compatibility
pydantic==2.11.2  # Must be >=2.11.0 for mcp 1.20.0
pydantic-settings==2.6.1  # Must be >=2.5.2 for mcp 1.20.0
python-dotenv==1.0.0

# Configuration
toml==0.10.2
pyyaml==6.0.1

# Security
cryptography==42.0.0
bcrypt==4.1.2

# Process Management
psutil>=5.9.0

# Monitoring - pinned for pyannote.audio compatibility
prometheus-client==0.19.0
opentelemetry-api==1.34.0  # Must be >=1.34.0 for pyannote.audio
opentelemetry-sdk==1.34.0
opentelemetry-instrumentation-fastapi==0.55b0

# Rate Limiting
slowapi==0.1.9

# WebSockets
websockets==15.0
python-socketio==5.11.0

# Supabase Dependencies
supabase==2.7.4
redis==5.0.8

# Async File I/O
aiofiles==23.2.1

# Data Validation
email-validator==2.1.0

# Utilities
python-json-logger==2.0.7
tenacity==8.2.3
pynput>=1.7.7
uv>=0.1.0

# MCP - Model Context Protocol
mcp==1.20.0
slack_sdk>=3.27.0
telethon>=1.33.0

# Open Interpreter Dependencies - using compatible versions
shortuuid>=1.0.0
litellm>=1.0.0
rich==13.9.4  # Must be >=13.9.4 for pyannote.audio
inquirer>=3.1.3
astor>=0.8.1
git-python>=1.0.3
six>=1.16.0
tokentrim>=0.1.13
wget>=3.2
html2image>=2.0.4
send2trash>=1.8.2
ipykernel==6.29.5  # Pinned to prevent backtracking
ipython==8.30.0  # Pinned to prevent backtracking
jupyter-client==8.6.3  # Pinned to prevent backtracking
matplotlib==3.10.0  # Must be >=3.10.0 for pyannote.audio
tiktoken>=0.7.0
platformdirs>=4.2.0
google-generativeai>=0.7.1
pyperclip>=1.9.0
yaspin>=3.0.2
html2text>=2024.2.26
selenium>=4.24.0
webdriver-manager>=4.0.2
anthropic>=0.37.1
pyautogui>=0.9.54
typer>=0.12.5
scikit-learn>=1.0.0
opencv-python>=4.8.0
janus>=1.0.0

# Open Interpreter (external server)
# LEGAL-CLEAN: Open Interpreter runs as an external process with its own environment.
# Do NOT install `open-interpreter` into the backend venv: it requires Starlette>=0.37, which conflicts
# with this backend's FastAPI pin (0.109.x -> Starlette 0.35.x).

# TTS Integration
stream2sentence==0.3.1
resampy==0.4.3
pydub==0.25.1
pyttsx3==2.90
gtts==2.4.0
edge-tts==6.1.9

# Audio Processing - using compatible versions
pyannote.audio==4.0.3
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
librosa>=0.10.2
soundfile==0.13.1  # Must be >=0.13.1 for pyannote.audio

# Kokoro TTS dependencies
kokoro==0.9.4
pyaudio==0.2.14
fugashi==1.4.0
jaconv==0.4.0
mojimoji==0.0.13
pyopenjtalk==0.4.1
pypinyin==0.54.0
ordered_set==4.1.0
jieba==0.42.1
cn2an==0.5.23

# OCR Integration
filetype==1.2.0
markdownify==1.1.0
pypdfium2>=4.30.0
qwen-vl-utils>=0.0.14
qwen-tts==0.1.1
accelerate==1.12.0  # Must match qwen-tts
transformers==4.57.3
easyocr>=1.7.0

# Optional: Performance
orjson==3.9.12
ujson==5.9.0

# Optional: Production server
gunicorn==21.2.0

openwakeword>=0.6.0

# Backwards-compatible Keras
tf-keras==2.18.0  # Pinned to prevent backtracking
apscheduler==3.10.4

# BM25 Keyword Search
python-terrier>=0.10.0

# Aether-RAG chunking dependencies
llama-index-core>=0.12.0
llama-index-readers-file>=0.4.0

# Summarisation evaluation
sumy>=0.11.0
nltk==3.9.1  # Must match stream2sentence
