# Aether Inference - NVIDIA CUDA Dependencies
# Install: pip install -r requirements-cuda.txt

# ---------- Router dependencies (services.aether_inference.server) ----------
# These run the on-demand multi-model router that manages backend processes.
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.26.0

# ---------- Engine: vLLM (NVIDIA CUDA) ----------
vllm>=0.4.0

# ---------- Engine: llama-cpp (GGUF models on any device) ----------
# Enables serving GGUF-format models alongside CUDA safetensors.
# Drop .gguf files into models/ and the router auto-detects and serves them.
# For CUDA-accelerated GGUF: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
llama-cpp-python>=0.3.0

# ---------- Model downloads ----------
huggingface_hub>=0.20.0
