# xlwings Backend Service (Deprecated)

This FastAPI server is no longer used by Aether.
The backend now uses **on-demand, in-process** xlwings via `core/integrations/libraries/xlwings`.

## Status

- Deprecated: do not start this server.
- Use `computer.xlwings.*` tools (in-process) instead.
