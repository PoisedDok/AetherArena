# Aether OI Server Wrapper Environment (legal-clean)
#
# This environment is intentionally separate from the backend venv.
# Reason: upstream `open-interpreter` requires Starlette>=0.37, while backend FastAPI pin requires Starlette 0.35.x.
#
# Install into a dedicated venv (see scripts/setup_oi_server_venv.sh).

open-interpreter>=0.4.0

# OI server runtime deps (upstream packaging may not include these consistently)
fastapi>=0.110.0
uvicorn>=0.27.0
janus>=1.0.0

# --------------------------------------------------------------------------
# Defensive transitive dependencies
#
# pip's resolver can leave these uninstalled due to metadata changes or
# partial resolution.  Declaring them explicitly guarantees a working venv
# on every fresh device install.
# --------------------------------------------------------------------------

# jupyter_client.session uses msgpack.packb at module level.
# jupyter_client >= 8.6 dropped msgpack from hard Requires but still imports it.
msgpack>=1.0.0

# jupyter_client uses orjson.OPT_NAIVE_UTC at module level but pip does not
# always resolve it as a transitive dependency.
orjson>=3.10.1

# Jupyter kernel transport layer (ipykernel + jupyter_client depend on these)
pyzmq>=25.0.0
tornado>=6.2

# Needed because the wrapper imports backend config + tool bridge modules directly from the repo.
pydantic>=2.5.3
toml==0.10.2
pyyaml==6.0.1
httpx>=0.26.0

# Optional deps used by some Open Interpreter builds / integrations.
beautifulsoup4>=4.12.0

# Excel automation and data analysis (required by GURU profile tools)
xlwings>=0.33.19
pandas>=2.2.3
numpy>=2.2.6
openpyxl>=3.1.5
matplotlib>=3.10.7
