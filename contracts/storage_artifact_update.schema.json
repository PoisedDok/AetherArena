{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aether/contracts/storage_artifact_update.schema.json",
  "title": "Aether Storage - Update Artifact Message ID",
  "type": "object",
  "required": ["artifact_id", "message_id"],
  "properties": {
    "artifact_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable artifact id generated at artifact creation"
    },
    "message_id": {
      "type": "string",
      "pattern": "^[0-9a-fA-F-]{36}$",
      "description": "PostgreSQL UUID of the persisted message"
    },
    "chat_id": {
      "type": "string",
      "pattern": "^[0-9a-fA-F-]{36}$",
      "description": "Optional chat UUID for contextual validation"
    }
  },
  "additionalProperties": false
}


